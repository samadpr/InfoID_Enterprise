namespace InfoID.Domain.Common.Enums;

public enum TemplateCategory
{
    School, Corporate, Event, Hospital
}

public enum CardSide
{
    Front, Back
}

public enum TemplateElementType
{
    Text, Image, Shape, QRCode, Barcode, DynamicField
}
